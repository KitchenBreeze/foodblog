name: KitchenBreeze Build

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Repository auschecken
        uses: actions/checkout@v4

      - name: Test
        run: echo "KitchenBreeze Build erfolgreich gestartet!"